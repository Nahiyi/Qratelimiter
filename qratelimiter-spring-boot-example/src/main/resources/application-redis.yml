spring:
  redis:
    host: localhost
    port: 6379
  data:
    redis:
      host: localhost
      port: 6379

clazs:
  ratelimiter:
    storage: redis
    redis:
      key-prefix: "qratelimiter:example:"
